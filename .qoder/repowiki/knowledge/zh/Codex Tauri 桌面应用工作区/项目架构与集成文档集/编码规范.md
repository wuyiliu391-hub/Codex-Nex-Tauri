- 文档以分层编号（L0–L4）统一描述职责边界，并在表格中列出允许/禁止行为作为硬约束。
- 对官方协议的任何断言必须追溯到 `generate-ts` / `generate-json-schema` 输出或 `APPSERVER-METHOD-INVENTORY-*.md`，禁止手写猜测 RPC 方法名。
- 变更历史采用 `compose/change-notes/<主题>.md` 的单文件单变更条目形式，便于逐条审阅。
- UIA/UAT 相关文档将文字大纲（`outlines-t41/*.txt`）与对应截图（`shots-t41/t41-*.png`）按页面一一对应存放。
- 涉及二进制或运行时行为的文档明确标注已知限制与降级路径（如无 engine binary 时 shell 仍可启动）。