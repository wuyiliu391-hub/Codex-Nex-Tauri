Rust workspace（edition 2024，clippy/workspace.lints 统一 lint 规则）、Tauri 2 壳、Vite + React 19 前端、Node/npm 脚本、Windows MSI 打包；外部依赖通过 `[workspace.dependencies]` 集中管理并在 `patch.crates-io` 中指向 openai-oss-forks 分支。