CI 流水线：`lint-check` 执行前端 typecheck/rustfmt，`build-fast`/`build-release` 下载官方 `codex-app-server-*.exe` sidecar 后运行 `cargo tauri build -- --no-default-features` 仅编译壳；本地开发可进入 `frontend/` 用 npm 构建并通过 docs/BROWSER-DEV.md 的浏览器调试模式绕过 Rust 编译。