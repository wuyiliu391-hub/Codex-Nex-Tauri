- 所有面向前端的交互统一通过 Tauri 命令（`commands/*.rs`）暴露，业务逻辑集中在对应命令文件中
- sidecar 进程生命周期与事件通道封装在 `codex/` 子模块内，对外仅暴露 adapter/client/events 接口
- 错误处理统一使用 thiserror 定义领域错误类型，上层用 anyhow 包装以便传播
- 跨进程/跨模块通信采用 serde 序列化结构体，JSON 作为协议载体