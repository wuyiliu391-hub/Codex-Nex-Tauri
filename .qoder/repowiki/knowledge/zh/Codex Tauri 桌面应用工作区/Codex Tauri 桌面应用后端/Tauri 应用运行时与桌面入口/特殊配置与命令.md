Windows 下通过编译期条件 `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]` 隐藏控制台窗口；日志级别可通过环境变量 `RUST_LOG` 或 `tracing_subscriber::EnvFilter::try_from_default_env()` 覆盖，默认 fallback 为 `info,codex_tauri=debug`；应用数据默认落盘路径为系统配置目录下的 `CodexDesktop/shell-state.json`。