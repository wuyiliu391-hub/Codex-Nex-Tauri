Tauri v2（`tauri::Builder`、`tauri::generate_handler!`、`tauri::generate_context!`），配合 `tauri_plugin_dialog`、`tauri_plugin_shell`、`tauri_plugin_fs`、`tauri_plugin_store`；日志使用 `tracing_subscriber` 并通过 `EnvFilter` 从环境变量读取过滤规则；状态序列化使用 `serde` + `serde_json`，进程间通信通过 `codex::EngineHandle` 启动的 WebSocket 侧进程。