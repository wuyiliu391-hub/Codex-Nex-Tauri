- 所有 Rust 依赖与版本号集中在根 `Cargo.toml` 的 `[workspace.dependencies]`，子 crate 通过名称引用而不写版本
- Clippy 规则在根 `[workspace.lints.clippy]` 统一声明为 warn，各 crate 继承而非重复配置
- UI 控件必须走真实 Tauri IPC，禁止使用假回调或占位成功响应
- 与官方引擎的交互严格遵循 `app-server generate-ts` 生成的协议，不得手写 RPC 契约