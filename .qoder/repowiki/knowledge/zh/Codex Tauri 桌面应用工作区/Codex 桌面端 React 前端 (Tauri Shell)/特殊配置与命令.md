`npm run dev` 启动 Vite 开发服务器（端口 1420）；`npm run build` 先执行 `tsc --noEmit` 类型检查再构建到 `dist/`；`npm run dev:browser` 启用 `browser` 模式，将 `@tauri-apps/api` 别名到 `devbridge/*` 并通过 WS 代理连接真实 codex-app-server（默认 `ws://127.0.0.1:17457`，可通过 `CODEX_APP_SERVER_WS` 覆盖）。