[package]
name = "codex-tauri"
version = "0.1.0"
description = "Codex desktop shell (Tauri) powered by official codex-app-server sidecar"
authors = ["Codex Contributors"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "codex_tauri_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon", "protocol-asset"] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
tauri-plugin-fs = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "process", "io-util", "net"] }
tokio-tungstenite = { version = "0.24", features = ["native-tls"] }
futures-util = "0.3"
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "1"
anyhow = "1"
base64 = "0.22"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
dirs = "5"
chrono = { version = "0.4", features = ["serde"] }
notify-rust = "4"
cron = "0.12"
walkdir = "2"
regex = "1"

# ─── Optional in-process backend (OFF by default) ─────────────────────────────
# Pulling these path deps requires the official codex-rust tree next to this
# repo and links the entire 100+ crate workspace. Cloud CI must NOT enable
# this feature — shell-only builds stay green without the official tree.
# See docs/RUST_BACKEND.md Mode B.
#
# codex-app-server-protocol = { path = "../../codex-rust-v0.154.0/codex-rs/app-server-protocol", optional = true }
# codex-app-server-client   = { path = "../../codex-rust-v0.154.0/codex-rs/app-server-client",   optional = true }

[features]
default = []
# in-process = ["dep:codex-app-server-protocol", "dep:codex-app-server-client"]

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
